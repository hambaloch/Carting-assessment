# Backend – copy to server/.env and fill in.
# Don't commit .env; use this so the team knows what's required.

# Frontend URL (CORS + Stripe redirects)
ORIGIN=http://localhost:5173

# MongoDB
MONGO_URI=mongodb://localhost:27017/techstep

# Auth (required for login/signup)
JWT_SECRET=your-secret-at-least-32-chars

# Stripe checkout (required for cart checkout)
STRIPE_KEY=sk_test_...

# Optional: password reset email (nodemailer)
# FRONTEND_URL=http://localhost:5173
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=
# EMAIL_PASS=
